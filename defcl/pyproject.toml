[project]
name = "define-defcl"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
    "lark",
    "protobuf",
]

[dependency-groups]
dev = [
    "pytest>=6",
]

[tool.basedpyright]
exclude = ["bazel-*"]

[tool.ruff]
extend = "../pyproject.toml"
