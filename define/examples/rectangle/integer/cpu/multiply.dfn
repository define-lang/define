define the encoding_operation<define-lang.org:rectangle:/integer/cpu/multiply> {
    it implements the operation</integer/multiply>.

    define the view<a> {
        it may only contain particles where {
            it has the encoding</integer/cpu>.
        }
    }
    define the view<b> {
        it may only contain particles where {
            it has the encoding</integer/cpu>.
        }
    }
    define the view<result> {
        it may only contain particles where {
            it has the encoding</integer/cpu>.
        }
    }

    it does {
        execute the computer operation.
    }
}
