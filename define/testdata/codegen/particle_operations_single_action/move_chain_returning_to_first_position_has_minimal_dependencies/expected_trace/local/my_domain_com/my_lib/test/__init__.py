"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Test(literal.Action):

    @override
    def run(self):
        a = literal.LocalPosition(
            "position<a>",
        )
        b = literal.LocalPosition(
            "position<b>",
        )
        c = literal.LocalPosition(
            "position<c>",
        )
        d = literal.LocalPosition(
            "position<d>",
        )
        a.create_particle()
        literal.record_operation("test.create(a)")
        a.move_particle_to(
            b
        )
        literal.record_operation("test.move(a, b)")
        b.move_particle_to(
            c
        )
        literal.record_operation("test.move(b, c)")
        c.move_particle_to(
            d
        )
        literal.record_operation("test.move(c, d)")
        d.move_particle_to(
            a
        )
        literal.record_operation("test.move(d, a)")
        a.destroy_particle()
        literal.record_operation("test.destroy(a)")
