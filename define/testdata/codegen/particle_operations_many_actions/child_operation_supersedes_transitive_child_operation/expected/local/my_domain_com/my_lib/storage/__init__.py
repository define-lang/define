"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Storage(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
            ],
        )

    @override
    def run(self):
        local_item = literal.LocalPosition(
            "position<local_item>",
        )
        local_item.create_particle()
        local_item.destroy_particle()
