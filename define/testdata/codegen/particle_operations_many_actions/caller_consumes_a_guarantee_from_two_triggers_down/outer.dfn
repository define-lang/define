define the potential action<my.domain.com:my_lib:/outer> {
    define the position<trigger_pos>.
    define the position<gw> {
        it may only contain particles where {
            it has the action</middle>.
        }
    }
    define the position<out>.
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        create a particle in position<gw>::action</middle>::position<igw>.
        create a particle in position<gw>::action</middle>::position<trigger_pos>.
        move the particle in position<gw>::action</middle>::position<out> to position<out>.
        destroy the particle in position<gw>::action</middle>::position<trigger_pos>.
        destroy the particle in position<gw>.
    }
}
