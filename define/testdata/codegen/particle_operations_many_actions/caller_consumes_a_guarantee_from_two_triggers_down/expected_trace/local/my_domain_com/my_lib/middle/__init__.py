"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.inner
import local.my_domain_com.my_lib.inner_result


class MiddleDestructionContracts:
    def run_destructors_position_igw(self, _particle: literal.Particle):
        pass

    def destroy_position_igw(self, _particle: literal.Particle):
        pass


_DEFAULT_DESTRUCTION_CONTRACTS = MiddleDestructionContracts()


class Middle(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
                literal.LocalPosition(
                    "position<igw>",
                    constraints=(
                        local.my_domain_com.my_lib.inner.Inner,
                        local.my_domain_com.my_lib.inner_result.InnerResult,
                    ),
                ),
                literal.LocalPosition("position<out>"),
            ],
        )

    @override
    def run(self, destruction_contracts: MiddleDestructionContracts = _DEFAULT_DESTRUCTION_CONTRACTS):
        self.get_interface_position(
            "position<igw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<trigger_pos>"
        ).create_particle()
        literal.record_operation("middle.create(igw::/inner::trigger_pos)")
        self.get_interface_position(
            "position<igw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).run()
        self.get_interface_position(
            "position<igw>"
        ).particle.get_position(
            local.my_domain_com.my_lib.inner_result.InnerResult
        ).move_particle_to(
            self.get_interface_position(
                "position<out>"
            )
        )
        literal.record_operation("middle.move(igw::/inner_result, out)")
        self.get_interface_position(
            "position<igw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<trigger_pos>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(igw::/inner::trigger_pos)")
        destruction_contracts.run_destructors_position_igw(
            self.get_interface_position(
                "position<igw>"
            ).particle
        )
        destruction_contracts.destroy_position_igw(
            self.get_interface_position(
                "position<igw>"
            ).particle
        )
        self.get_interface_position(
            "position<igw>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(igw)")
