"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class BrewDestructionContracts:
    def run_destructors_position_water(self, _particle: literal.Particle):
        pass

    def destroy_position_water(self, _particle: literal.Particle):
        pass


_DEFAULT_DESTRUCTION_CONTRACTS = BrewDestructionContracts()


class Brew(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<grounds>"),
                literal.LocalPosition("position<water>"),
                literal.LocalPosition("position<cup>"),
                literal.LocalPosition("position<spent_puck>"),
            ],
        )

    @override
    def run(self, destruction_contracts: BrewDestructionContracts = _DEFAULT_DESTRUCTION_CONTRACTS):
        self.get_interface_position(
            "position<cup>"
        ).create_particle()
        destruction_contracts.run_destructors_position_water(
            self.get_interface_position(
                "position<water>"
            ).particle
        )
        destruction_contracts.destroy_position_water(
            self.get_interface_position(
                "position<water>"
            ).particle
        )
        self.get_interface_position(
            "position<water>"
        ).destroy_particle()
        self.get_interface_position(
            "position<grounds>"
        ).move_particle_to(
            self.get_interface_position(
                "position<spent_puck>"
            )
        )
