"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Heat(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<cold_water>"),
                literal.LocalPosition("position<hot_water>"),
            ],
        )

    @override
    def run(self):
        self.get_interface_position(
            "position<cold_water>"
        ).move_particle_to(
            self.get_interface_position(
                "position<hot_water>"
            )
        )
