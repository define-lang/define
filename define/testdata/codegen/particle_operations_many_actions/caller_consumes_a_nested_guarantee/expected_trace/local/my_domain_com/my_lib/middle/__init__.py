"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.inner


class MiddleDestructionContracts:
    def run_destructors_position_gw(self, _particle: literal.Particle):
        pass

    def destroy_position_gw(self, _particle: literal.Particle):
        pass


_DEFAULT_DESTRUCTION_CONTRACTS = MiddleDestructionContracts()


class Middle(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
                literal.LocalPosition(
                    "position<gw>",
                    constraints=(
                        local.my_domain_com.my_lib.inner.Inner,
                    ),
                ),
                literal.LocalPosition("position<out>"),
            ],
        )

    @override
    def run(self, destruction_contracts: MiddleDestructionContracts = _DEFAULT_DESTRUCTION_CONTRACTS):
        self.get_interface_position(
            "position<gw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<trigger_pos>"
        ).create_particle()
        literal.record_operation("middle.create(gw::/inner::trigger_pos)")
        self.get_interface_position(
            "position<gw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).run()
        self.get_interface_position(
            "position<gw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<out>"
        ).move_particle_to(
            self.get_interface_position(
                "position<out>"
            )
        )
        literal.record_operation("middle.move(gw::/inner::out, out)")
        self.get_interface_position(
            "position<gw>"
        ).particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<trigger_pos>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(gw::/inner::trigger_pos)")
        destruction_contracts.run_destructors_position_gw(
            self.get_interface_position(
                "position<gw>"
            ).particle
        )
        destruction_contracts.destroy_position_gw(
            self.get_interface_position(
                "position<gw>"
            ).particle
        )
        self.get_interface_position(
            "position<gw>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(gw)")
