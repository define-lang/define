define the potential action<my.domain.com:my_lib:/outer> {
    it also assigns the position</holder>.
    it also assigns the action</middle>.
    define the position<trigger_pos>.
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        destroy the particle in position</holder>::position</a>.
        create a particle in action</middle>::position<trigger_pos>.
        destroy the particle in action</middle>::position<trigger_pos>.
    }
}
