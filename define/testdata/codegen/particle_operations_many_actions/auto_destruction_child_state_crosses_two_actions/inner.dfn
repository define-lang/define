define the potential action<my.domain.com:my_lib:/inner> {
    define the position<inner_run>.
    it happens when {
        the position<inner_run> has a particle.
    } and it does {
        define the position<local>.
        move the particle in position<inner_run> to position<local>.
    }
}
