"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Worker(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<run>"),
                literal.LocalPosition("position<input>"),
                literal.LocalPosition("position<output>"),
            ],
        )

    @override
    def run(self):
        self.get_interface_position(
            "position<input>"
        ).move_particle_to(
            self.get_interface_position(
                "position<output>"
            )
        )
        literal.record_operation("worker.move(input, output)")
        self.get_interface_position(
            "position<run>"
        ).destroy_particle()
        literal.record_operation("worker.destroy(run)")
