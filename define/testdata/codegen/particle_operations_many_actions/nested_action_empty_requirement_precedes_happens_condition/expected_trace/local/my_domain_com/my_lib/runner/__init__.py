"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.middle


class Runner(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<run>"),
            ],
        )

    @override
    def run(self):
        wrapper = literal.LocalPosition(
            "position<wrapper>",
            constraints=(
                local.my_domain_com.my_lib.middle.Middle,
            ),
        )
        wrapper.create_particle()
        literal.record_operation("runner.create(wrapper)")
        wrapper.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<box>"
        ).create_particle()
        literal.record_operation("runner.create(wrapper::/middle::box)")
        wrapper.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<run>"
        ).create_particle()
        literal.record_operation("runner.create(wrapper::/middle::run)")
        wrapper.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).run()
        wrapper.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<final>"
        ).destroy_particle()
        literal.record_operation("runner.destroy(wrapper::/middle::final)")
        wrapper.destroy_particle()
        literal.record_operation("runner.destroy(wrapper)")
