"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.middle
import local.my_domain_com.my_lib.result_value


class Outer(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
                literal.LocalPosition(
                    "position<source>",
                    constraints=(
                        local.my_domain_com.my_lib.result_value.ResultValue,
                    ),
                ),
                literal.LocalPosition(
                    "position<destination>",
                    constraints=(
                        local.my_domain_com.my_lib.result_value.ResultValue,
                    ),
                ),
            ],
        )

    @override
    def run(self):
        middle_holder = literal.LocalPosition(
            "position<middle_holder>",
            constraints=(
                local.my_domain_com.my_lib.middle.Middle,
            ),
        )
        result_holder = literal.LocalPosition(
            "position<result_holder>",
        )
        middle_holder.create_particle()
        literal.record_operation("outer.create(middle_holder)")
        self.get_interface_position(
            "position<source>"
        ).move_particle_to(
            middle_holder.particle.get_action(
                local.my_domain_com.my_lib.middle.Middle
            ).get_interface_position(
                "position<inner_parent>"
            )
        )
        literal.record_operation("outer.move(source, middle_holder::/middle::inner_parent)")
        middle_holder.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<trigger_pos>"
        ).create_particle()
        literal.record_operation("outer.create(middle_holder::/middle::trigger_pos)")
        middle_holder.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).run()
        middle_holder.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<inner_parent>"
        ).particle.get_position(
            local.my_domain_com.my_lib.result_value.ResultValue
        ).move_particle_to(
            result_holder
        )
        literal.record_operation("outer.move(middle_holder::/middle::inner_parent::/result_value, result_holder)")
        middle_holder.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<inner_parent>"
        ).move_particle_to(
            self.get_interface_position(
                "position<destination>"
            )
        )
        literal.record_operation("outer.move(middle_holder::/middle::inner_parent, destination)")
        result_holder.move_particle_to(
            self.get_interface_position(
                "position<destination>"
            ).particle.get_position(
                local.my_domain_com.my_lib.result_value.ResultValue
            )
        )
        literal.record_operation("outer.move(result_holder, destination::/result_value)")
        middle_holder.particle.get_action(
            local.my_domain_com.my_lib.middle.Middle
        ).get_interface_position(
            "position<trigger_pos>"
        ).destroy_particle()
        literal.record_operation("outer.destroy(middle_holder::/middle::trigger_pos)")
        middle_holder.destroy_particle()
        literal.record_operation("outer.destroy(middle_holder)")
