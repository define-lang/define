"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Child(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
            ],
        )

    @override
    def run(self):
        scratch = literal.LocalPosition(
            "position<scratch>",
        )
        scratch.create_particle()
        literal.record_operation("child.create(scratch)")
        scratch.destroy_particle()
        literal.record_operation("child.destroy(scratch)")
        self.get_interface_position(
            "position<trigger_pos>"
        ).destroy_particle()
        literal.record_operation("child.destroy(trigger_pos)")
