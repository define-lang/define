"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.child
import local.my_domain_com.my_lib.marker


class Middle(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
                literal.LocalPosition(
                    "position<source>",
                    constraints=(
                        local.my_domain_com.my_lib.child.Child,
                        local.my_domain_com.my_lib.marker.Marker,
                    ),
                ),
                literal.LocalPosition(
                    "position<holder>",
                    constraints=(
                        local.my_domain_com.my_lib.marker.Marker,
                    ),
                ),
            ],
        )

    @override
    def run(self):
        self.get_interface_position(
            "position<source>"
        ).particle.get_action(
            local.my_domain_com.my_lib.child.Child
        ).get_interface_position(
            "position<trigger_pos>"
        ).create_particle()
        literal.record_operation("middle.create(source::/child::trigger_pos)")
        self.get_interface_position(
            "position<source>"
        ).particle.get_action(
            local.my_domain_com.my_lib.child.Child
        ).run()
        self.get_interface_position(
            "position<source>"
        ).move_particle_to(
            self.get_interface_position(
                "position<holder>"
            )
        )
        literal.record_operation("middle.move(source, holder)")
        self.get_interface_position(
            "position<holder>"
        ).particle.get_position(
            local.my_domain_com.my_lib.marker.Marker
        ).destroy_particle()
        literal.record_operation("middle.destroy(holder::/marker)")
        self.get_interface_position(
            "position<holder>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(holder)")
