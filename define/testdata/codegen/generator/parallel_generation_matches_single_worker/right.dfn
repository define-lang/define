define the potential action<my.domain.com:my_lib:/right> {
    it also assigns the action</shared>.
    define the position<trigger>.
    it happens when {
        the position<trigger> has a particle.
    } and it does {
        create a particle in action</shared>::position<trigger>.
        destroy the particle in position<trigger>.
    }
}
