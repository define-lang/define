define the potential action<my.domain.com:my_lib:/type_error> {
    define the position<pp>.
    it happens when {
        the position<pp> has a particle.
    } and it does {
        define the position<noop>.
        create a particle in position<noop>.
    }
}
