"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class TypeErrorDestructionContracts:
    def run_destructors_position_pp(self, _particle: literal.Particle):
        pass

    def destroy_position_pp(self, _particle: literal.Particle):
        pass


_DEFAULT_DESTRUCTION_CONTRACTS = TypeErrorDestructionContracts()


class TypeError(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<pp>"),
            ],
        )

    @override
    def run(self, destruction_contracts: TypeErrorDestructionContracts = _DEFAULT_DESTRUCTION_CONTRACTS):
        noop = literal.LocalPosition(
            "position<noop>",
        )
        noop.create_particle()
        destruction_contracts.run_destructors_position_pp(
            self.get_interface_position(
                "position<pp>"
            ).particle
        )
        destruction_contracts.destroy_position_pp(
            self.get_interface_position(
                "position<pp>"
            ).particle
        )
        self.get_interface_position(
            "position<pp>"
        ).destroy_particle()
        noop.destroy_particle()
