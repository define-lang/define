define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</kick_off>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</kick_off>::position<trigger>.
    }
}
