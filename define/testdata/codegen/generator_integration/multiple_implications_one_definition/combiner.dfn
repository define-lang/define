define the potential action<my.domain.com:my_lib:/combiner> {
    it also assigns the position</first_marker>.
    it also assigns the position</second_marker>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in position</first_marker>.
        create a particle in position</second_marker>.
    }
}
