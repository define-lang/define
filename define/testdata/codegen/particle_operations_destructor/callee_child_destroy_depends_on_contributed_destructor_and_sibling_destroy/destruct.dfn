define the potential action<my.domain.com:my_lib:/destruct> {
    it also assigns the position</required>.
    it happens when {
        this particle is being destroyed.
    } and it does {
        create a particle in position</required>::position</work>.
        destroy the particle in position</required>::position</work>.
    }
}
