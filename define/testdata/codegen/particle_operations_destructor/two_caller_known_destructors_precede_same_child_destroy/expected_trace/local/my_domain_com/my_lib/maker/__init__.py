"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Maker(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
                literal.LocalPosition("position<result>"),
            ],
        )

    @override
    def run(self):
        self.get_interface_position(
            "position<result>"
        ).create_particle()
        literal.record_operation("maker.create(result)")
        self.get_interface_position(
            "position<trigger_pos>"
        ).destroy_particle()
        literal.record_operation("maker.destroy(trigger_pos)")
