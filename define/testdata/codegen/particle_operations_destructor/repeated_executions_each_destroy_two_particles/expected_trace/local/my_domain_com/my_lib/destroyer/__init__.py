"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Destroyer(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<first>"),
                literal.LocalPosition("position<second>"),
                literal.LocalPosition("position<run>"),
            ],
        )

    @override
    def run(self):
        used_run = literal.LocalPosition(
            "position<used_run>",
        )
        self.get_interface_position(
            "position<run>"
        ).move_particle_to(
            used_run
        )
        literal.record_operation("destroyer.move(run, used_run)")
        self.get_interface_position(
            "position<first>"
        ).destroy_particle()
        literal.record_operation("destroyer.destroy(first)")
        self.get_interface_position(
            "position<second>"
        ).destroy_particle()
        literal.record_operation("destroyer.destroy(second)")
        used_run.destroy_particle()
        literal.record_operation("destroyer.destroy(used_run)")
