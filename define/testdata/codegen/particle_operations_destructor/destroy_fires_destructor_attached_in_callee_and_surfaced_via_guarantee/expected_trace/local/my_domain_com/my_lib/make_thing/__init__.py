"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.destructor


class MakeThing(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<result>"),
                literal.LocalPosition("position<run>"),
            ],
        )

    @override
    def run(self):
        temp = literal.LocalPosition(
            "position<temp>",
            constraints=(
                local.my_domain_com.my_lib.destructor.Destructor,
            ),
        )
        temp.create_particle()
        literal.record_operation("make_thing.create(temp)")
        temp.move_particle_to(
            self.get_interface_position(
                "position<result>"
            )
        )
        literal.record_operation("make_thing.move(temp, result)")
