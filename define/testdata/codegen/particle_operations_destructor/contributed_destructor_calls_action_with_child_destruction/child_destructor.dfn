define the potential action<my.domain.com:my_lib:/child_destructor> {
    it also assigns the position</child_marker>.
    it happens when {
        this particle is being destroyed.
    } and it does {
        create a particle in position</child_marker>.
        destroy the particle in position</child_marker>.
    }
}
