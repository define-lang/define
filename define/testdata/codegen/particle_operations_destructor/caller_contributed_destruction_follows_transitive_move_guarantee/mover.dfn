define the potential action<my.domain.com:my_lib:/mover> {
    define the position<run>.
    define the position<result>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        move the particle in position<run> to position<result>.
    }
}
