"""Generated by the Define compiler."""

from typing import ClassVar, override

from define.runtime import literal

import local.my_domain_com.my_lib.item


class WorkerDestructionContracts:
    def run_destructors_global_position_item(self, _particle: literal.Particle):
        pass

    def destroy_global_position_item(self, _particle: literal.Particle):
        pass

    def run_destructors_position_item(self, _particle: literal.Particle):
        pass

    def destroy_position_item(self, _particle: literal.Particle):
        pass


_DEFAULT_DESTRUCTION_CONTRACTS = WorkerDestructionContracts()


class Worker(literal.Action):
    implied_qualities: ClassVar[tuple[type[literal.Quality], ...]] = (
        local.my_domain_com.my_lib.item.Item,
    )

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<item>"),
                literal.LocalPosition("position<run>"),
            ],
        )

    @override
    def run(self, destruction_contracts: WorkerDestructionContracts = _DEFAULT_DESTRUCTION_CONTRACTS):
        destruction_contracts.run_destructors_global_position_item(
            self.on_particle.get_position(
                local.my_domain_com.my_lib.item.Item
            ).particle
        )
        destruction_contracts.destroy_global_position_item(
            self.on_particle.get_position(
                local.my_domain_com.my_lib.item.Item
            ).particle
        )
        self.on_particle.get_position(
            local.my_domain_com.my_lib.item.Item
        ).destroy_particle()
        destruction_contracts.run_destructors_position_item(
            self.get_interface_position(
                "position<item>"
            ).particle
        )
        destruction_contracts.destroy_position_item(
            self.get_interface_position(
                "position<item>"
            ).particle
        )
        self.get_interface_position(
            "position<item>"
        ).destroy_particle()
