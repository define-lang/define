"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.carrier
import local.my_domain_com.my_lib.outer


class Wrapper(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<run>",
                    constraints=(
                        local.my_domain_com.my_lib.outer.Outer,
                        local.my_domain_com.my_lib.carrier.Carrier,
                    ),
                ),
            ],
        )

    @override
    def run(self):
        self.get_interface_position(
            "position<run>"
        ).particle.get_position(
            local.my_domain_com.my_lib.carrier.Carrier
        ).move_particle_to(
            self.get_interface_position(
                "position<run>"
            ).particle.get_action(
                local.my_domain_com.my_lib.outer.Outer
            ).get_interface_position(
                "position<run>"
            )
        )
        literal.record_operation("wrapper.move(run::/carrier, run::/outer::run)")
        self.get_interface_position(
            "position<run>"
        ).particle.get_action(
            local.my_domain_com.my_lib.outer.Outer
        ).run()
        self.get_interface_position(
            "position<run>"
        ).destroy_particle()
        literal.record_operation("wrapper.destroy(run)")
