define the potential action<my.domain.com:my_lib:/worker> {
    it also assigns the position</a_b>.
    it also assigns the position</a/b>.
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        destroy the particle in position</a_b>.
        destroy the particle in position</a/b>.
    }
}
