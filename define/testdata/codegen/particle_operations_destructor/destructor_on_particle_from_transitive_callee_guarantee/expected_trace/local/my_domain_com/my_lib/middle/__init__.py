"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.destructor
import local.my_domain_com.my_lib.inner
import local.my_domain_com.my_lib.marker


class Middle(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<result>",
                    constraints=(
                        local.my_domain_com.my_lib.destructor.Destructor,
                        local.my_domain_com.my_lib.marker.Marker,
                    ),
                ),
                literal.LocalPosition("position<run>"),
            ],
        )

    @override
    def run(self):
        box = literal.LocalPosition(
            "position<box>",
            constraints=(
                local.my_domain_com.my_lib.inner.Inner,
            ),
        )
        held_marker = literal.LocalPosition(
            "position<held_marker>",
        )
        box.create_particle()
        literal.record_operation("middle.create(box)")
        box.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<run>"
        ).create_particle()
        literal.record_operation("middle.create(box::/inner::run)")
        box.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).run()
        box.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<result>"
        ).particle.get_position(
            local.my_domain_com.my_lib.marker.Marker
        ).move_particle_to(
            held_marker
        )
        literal.record_operation("middle.move(box::/inner::result::/marker, held_marker)")
        box.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<result>"
        ).move_particle_to(
            self.get_interface_position(
                "position<result>"
            )
        )
        literal.record_operation("middle.move(box::/inner::result, result)")
        held_marker.move_particle_to(
            self.get_interface_position(
                "position<result>"
            ).particle.get_position(
                local.my_domain_com.my_lib.marker.Marker
            )
        )
        literal.record_operation("middle.move(held_marker, result::/marker)")
        box.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<run>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(box::/inner::run)")
        box.destroy_particle()
        literal.record_operation("middle.destroy(box)")
