"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import standard.number.rational


class Test(literal.Action):

    @override
    def run(self):
        count = literal.LocalPosition(
            "position<count>",
            constraints=(
                standard.number.rational.NumberRational,
            ),
        )
        count.create_particle()
        literal.record_operation("test.create(count)")
        count.set_value(42)
        literal.record_operation("test.set_value(count, 42)")
        count.destroy_particle()
        literal.record_operation("test.destroy(count)")
