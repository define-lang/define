"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import standard.number.rational


class Test(literal.Action):

    @override
    def run(self):
        source = literal.LocalPosition(
            "position<source>",
            constraints=(
                standard.number.rational.NumberRational,
            ),
        )
        target = literal.LocalPosition(
            "position<target>",
            constraints=(
                standard.number.rational.NumberRational,
            ),
        )
        source.create_particle()
        literal.record_operation("test.create(source)")
        target.create_particle()
        literal.record_operation("test.create(target)")
        source.set_value(-5)
        literal.record_operation("test.set_value(source, -5)")
        target.set_value_from(
            source
        )
        literal.record_operation("test.set_value(target, source)")
        source.destroy_particle()
        literal.record_operation("test.destroy(source)")
        target.destroy_particle()
        literal.record_operation("test.destroy(target)")
