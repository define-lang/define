"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import standard.number.rational


class Test(literal.Action):

    @override
    def run(self):
        price = literal.LocalPosition(
            "position<price>",
            constraints=(
                standard.number.rational.NumberRational,
            ),
        )
        price.create_particle()
        literal.record_operation("test.create(price)")
        price.set_value(12.5)
        literal.record_operation("test.set_value(price, 12.5)")
        price.destroy_particle()
        literal.record_operation("test.destroy(price)")
