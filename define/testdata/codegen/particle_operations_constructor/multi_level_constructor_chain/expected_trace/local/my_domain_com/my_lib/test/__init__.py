"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.construct_b
import local.my_domain_com.my_lib.inner
import local.my_domain_com.my_lib.leaf


class Test(literal.Action):

    @override
    def run(self):
        box = literal.LocalPosition(
            "position<box>",
            constraints=(
                local.my_domain_com.my_lib.construct_b.ConstructB,
            ),
        )
        box.create_particle()
        literal.record_operation("test.create(box)")
        box.particle.get_action(
            local.my_domain_com.my_lib.construct_b.ConstructB
        ).run()
        box.particle.get_position(
            local.my_domain_com.my_lib.inner.Inner
        ).particle.get_position(
            local.my_domain_com.my_lib.leaf.Leaf
        ).destroy_particle()
        literal.record_operation("test.destroy(box::/inner::/leaf)")
        box.particle.get_position(
            local.my_domain_com.my_lib.inner.Inner
        ).destroy_particle()
        literal.record_operation("test.destroy(box::/inner)")
        box.destroy_particle()
        literal.record_operation("test.destroy(box)")
