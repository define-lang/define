"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.other


class Test(literal.Action):

    @override
    def run(self):
        sink = literal.LocalPosition(
            "position<sink>",
        )
        gateway = literal.LocalPosition(
            "position<gateway>",
            constraints=(
                local.my_domain_com.my_lib.other.Other,
            ),
        )
        gateway.create_particle()
        literal.record_operation("test.create(gateway)")
        gateway.particle.get_action(
            local.my_domain_com.my_lib.other.Other
        ).get_interface_position(
            "position<trigger_pos>"
        ).create_particle()
        literal.record_operation("test.create(gateway::/other::trigger_pos)")
        gateway.particle.get_action(
            local.my_domain_com.my_lib.other.Other
        ).run()
        gateway.particle.get_action(
            local.my_domain_com.my_lib.other.Other
        ).get_interface_position(
            "position<slot>"
        ).move_particle_to(
            sink
        )
        literal.record_operation("test.move(gateway::/other::slot, sink)")
        gateway.particle.get_action(
            local.my_domain_com.my_lib.other.Other
        ).get_interface_position(
            "position<trigger_pos>"
        ).create_particle()
        literal.record_operation("test.create(gateway::/other::trigger_pos)")
        gateway.particle.get_action(
            local.my_domain_com.my_lib.other.Other
        ).run()
        gateway.particle.get_action(
            local.my_domain_com.my_lib.other.Other
        ).get_interface_position(
            "position<slot>"
        ).destroy_particle()
        literal.record_operation("test.destroy(gateway::/other::slot)")
        sink.destroy_particle()
        literal.record_operation("test.destroy(sink)")
        gateway.destroy_particle()
        literal.record_operation("test.destroy(gateway)")
