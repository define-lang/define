"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Other(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger_pos>"),
            ],
        )

    @override
    def run(self):
        other_item = literal.LocalPosition(
            "position<other_item>",
        )
        other_item.create_particle()
        literal.record_operation("other.create(other_item)")
        other_item.destroy_particle()
        literal.record_operation("other.destroy(other_item)")
