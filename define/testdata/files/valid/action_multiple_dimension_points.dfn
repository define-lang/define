define the potential action<mv:define-lang.org:test_files:/valid/action_multiple_dimension_points> {
    define the position<first_pos>.
    define the position<second_pos>.
    it happens when {
        the position<first_pos> has a dimension point.
    } and it does {
        create a dimension point in position<second_pos>.
    }
}
