define the potential position<mv:define-lang.org:test_files:/valid/action_dimension_point_chain>.
define the potential action<mv:define-lang.org:test_files:/valid/action_dimension_point_chain> {
    define the position<local_pos> {
        it may only contain dimension points where {
            it has the position</valid/action_dimension_point_chain>.
        }
    }
    it happens when {
        the position<local_pos> has a dimension point.
    } and it does {
        create a dimension point in position<local_pos>::position</valid/action_dimension_point_chain>.
    }
}
