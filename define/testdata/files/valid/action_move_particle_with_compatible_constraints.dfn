define the potential position<mv:define-lang.org:test_files:/valid/action_move_particle_with_compatible_constraints>.
define the potential action<mv:define-lang.org:test_files:/valid/action_move_particle_with_compatible_constraints> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<from_pos> {
            it may only contain particles where {
                it has the position</valid/action_move_particle_with_compatible_constraints>.
                it has the position</valid/action_local_pos_requires_global>.
            }
        }
        define the position<to_pos> {
            it may only contain particles where {
                it has the position</valid/action_move_particle_with_compatible_constraints>.
            }
        }
        create a particle in position<from_pos>.
        move the particle in position<from_pos> to position<to_pos>.
    }
}
