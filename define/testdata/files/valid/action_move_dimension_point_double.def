define the potential action<mv:define-lang.org:test_files:/valid/action_move_dimension_point_double> {
    define the position<pos_a>.
    define the position<pos_b>.
    define the position<pos_c>.
    it happens when {
    } and it does {
        create a dimension point in position<pos_a>.
        move the dimension point in position<pos_a> to position<pos_b>.
        move the dimension point in position<pos_b> to position<pos_c>.
    }
}
