define the potential action<mv:define-lang.org:test_files:/valid/quality_implications/constructor_implies_action> {
    it also assigns the action</valid/minimal_action>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</valid/minimal_action>::position<pp>.
    }
}
