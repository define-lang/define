define the potential action<mv:define-lang.org:test_files:/invalid/dimension_points/move_single_action_reference> {
    define the position<to_pos>.
    it happens when {
    } and it does {
        move the dimension point in action<act_other> to position<to_pos>.
    }
}
