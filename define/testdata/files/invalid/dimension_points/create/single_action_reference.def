define the potential action<mv:define-lang.org:test_files:/invalid/dimension_points/create/single_action_reference> {
    define the position<run>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
        create a dimension point in action<act_other>.
    }
}
