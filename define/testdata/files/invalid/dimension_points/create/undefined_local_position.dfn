define the potential action<mv:define-lang.org:test_files:/invalid/dimension_points/create/undefined_local_position> {
    define the position<run>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
        create a dimension point in position<no_such_pos>.
    }
}
