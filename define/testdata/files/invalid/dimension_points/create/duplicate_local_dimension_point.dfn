define the potential action<mv:define-lang.org:test_files:/invalid/dimension_points/create/duplicate_local_dimension_point> {
    define the position<run>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
        define the position<my_pos>.
        create a dimension point in position<my_pos>.
        create a dimension point in position<my_pos>.
    }
}
