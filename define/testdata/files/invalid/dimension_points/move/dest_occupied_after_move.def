define the potential action<mv:define-lang.org:test_files:/invalid/dimension_points/move/dest_occupied_after_move> {
    it happens when {
    } and it does {
        define the position<pos_a>.
        define the position<pos_b>.
        create a dimension point in position<pos_a>.
        move the dimension point in position<pos_a> to position<pos_b>.
        create a dimension point in position<pos_b>.
    }
}
