define the potential position<mv:define-lang.org:test_files:/invalid/dimension_points/move/violates_dest_constraints_unconstrained_source>.
define the potential action<mv:define-lang.org:test_files:/invalid/dimension_points/move/violates_dest_constraints_unconstrained_source> {
    define the position<run>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
        define the position<from_pos>.
        define the position<to_pos> {
            it may only contain dimension points where {
                it has the position</invalid/dimension_points/move/violates_dest_constraints_unconstrained_source>.
            }
        }
        create a dimension point in position<from_pos>.
        move the dimension point in position<from_pos> to position<to_pos>.
    }
}
