define the potential action<mv:define-lang.org:test_files:/invalid/indentation/action_no_indentation> {
define the position<run>.
it happens when {
    the position<run> has a particle.
} and it does {
    define the position<_noop>.
    create a particle in position<_noop>.
}
}
