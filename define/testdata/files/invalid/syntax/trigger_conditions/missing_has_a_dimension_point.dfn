define the potential action<mv:define-lang.org:test_files:/invalid/syntax/trigger_conditions/missing_has_a_dimension_point> {
    define the position<run>.
    it happens when {
        the position<run>.
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
