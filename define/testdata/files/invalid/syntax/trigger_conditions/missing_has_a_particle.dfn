define the potential action<mv:define-lang.org:test_files:/invalid/syntax/trigger_conditions/missing_has_a_particle> {
    define the position<run>.
    it happens when {
        the position<run>.
    } and it does {
        define the position<_noop>.
        create a particle in position<_noop>.
    }
}
