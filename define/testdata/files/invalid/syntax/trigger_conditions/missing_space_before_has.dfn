define the potential action<mv:define-lang.org:test_files:/invalid/syntax/trigger_conditions/missing_space_before_has> {
    define the position<run>.
    it happens when {
        the position<run>has a dimension point.
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
