define the potential action<mv:define-lang.org:test_files:/invalid/syntax/trigger_conditions/trailing_chain_separator> {
    define the position<run>.
    it happens when {
        the position<run>:: has a dimension point.
    } and it does {
    }
}
