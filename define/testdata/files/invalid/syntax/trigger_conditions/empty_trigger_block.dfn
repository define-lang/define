define the potential action<mv:define-lang.org:test_files:/invalid/syntax/trigger_conditions/empty_trigger_block> {
    define the position<run>.
    it happens when {
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
