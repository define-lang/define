define the potential action<mv:define-lang.org:test_files:/invalid/syntax/action_blocks/trigger_and_action_wrong_line> {
    define the position<run>.
    it happens when {
        the position<run> has a dimension point.
    }
    and it does {
    }
}
