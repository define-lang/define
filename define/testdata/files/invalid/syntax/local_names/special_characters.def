define the potential action<mv:define-lang.org:test_files:/invalid/syntax/local_names/special_characters> {
    define the position<run>.
    define the position<my@pos>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
    }
}
