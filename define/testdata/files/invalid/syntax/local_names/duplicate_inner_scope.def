define the potential action<mv:define-lang.org:test_files:/invalid/syntax/local_names/duplicate_inner_scope> {
    it happens when {
    } and it does {
        define the position<same_name>.
        define the position<same_name>.
    }
}
