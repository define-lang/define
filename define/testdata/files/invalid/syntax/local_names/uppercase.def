define the potential action<mv:define-lang.org:test_files:/invalid/syntax/local_names/uppercase> {
    define the position<run>.
    define the position<BadName>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
    }
}
