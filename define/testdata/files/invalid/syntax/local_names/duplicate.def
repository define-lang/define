define the potential action<mv:define-lang.org:test_files:/invalid/syntax/local_names/duplicate> {
    define the position<same_name>.
    define the position<same_name>.
    it happens when {
    } and it does {
    }
}
