define the potential action<mv:define-lang.org:test_files:/invalid/syntax/local_names/leading_digit> {
    define the position<run>.
    define the position<1bad>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<_noop>.
        create a particle in position<_noop>.
    }
}
