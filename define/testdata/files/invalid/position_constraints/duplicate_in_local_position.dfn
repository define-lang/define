define the potential action<mv:define-lang.org:test_files:/invalid/position_constraints/duplicate_in_local_position> {
    define the position<inner> {
        it may only contain dimension points where {
            it has the position</valid/action_local_pos_requires_global>.
            it has the position</valid/action_local_pos_requires_global>.
        }
    }
    it happens when {
        the position<inner> has a dimension point.
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
