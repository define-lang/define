define the potential action<mv:define-lang.org:test_files:/invalid/references/unnecessary_self_reference> {
    define the position<run>.
    define the position<inner>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in action</invalid/references/unnecessary_self_reference>::position<inner>.
    }
}
