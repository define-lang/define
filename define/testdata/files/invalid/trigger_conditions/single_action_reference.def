define the potential action<mv:define-lang.org:test_files:/invalid/trigger_conditions/single_action_reference> {
    define the position<run>.
    it happens when {
        the action<act_other> has a dimension point.
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
