define the potential action<mv:define-lang.org:test_files:/invalid/trigger_conditions/invalid_local_name_format> {
    it happens when {
        the position<BAD> has a dimension point.
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
