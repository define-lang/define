define the potential action<mv:define-lang.org:test_files:/invalid/trigger_conditions/undefined_local_position> {
    it happens when {
        the position<unknown> has a dimension point.
    } and it does {
    }
}
