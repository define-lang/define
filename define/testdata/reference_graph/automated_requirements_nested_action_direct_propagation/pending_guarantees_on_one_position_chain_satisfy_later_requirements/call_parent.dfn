define the potential action<my.domain.com:my_lib:/call_parent> {
    it also assigns the action</fill_parent>.
    define the position<trigger_pos>.
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        create a particle in action</fill_parent>::position<trigger_pos>.
    }
}
