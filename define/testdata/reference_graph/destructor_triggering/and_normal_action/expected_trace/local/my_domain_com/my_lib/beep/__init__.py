"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Beep(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<trigger>"),
            ],
        )

    @override
    def run(self):
        _noop = literal.LocalPosition(
            "position<_noop>",
        )
        _noop.create_particle()
        literal.record_operation("beep.create(_noop)")
        _noop.destroy_particle()
        literal.record_operation("beep.destroy(_noop)")
