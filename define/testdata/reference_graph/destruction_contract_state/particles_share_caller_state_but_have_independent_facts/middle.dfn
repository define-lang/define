define the potential action<my.domain.com:my_lib:/middle> {
    it also assigns the action</destroyer>.
    define the position<run> {
        it may only contain particles where {
            it has the position</child>.
        }
    }
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<temporary>.
        move the particle in position<run>::position</child> to position<temporary>.
        move the particle in position<temporary> to position<run>::position</child>.
        move the particle in position<run> to action</destroyer>::position<run>.
    }
}
