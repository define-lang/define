define the potential action<mv:define-lang.org:main_lib:/middle> {
    define the position<trigger_pos>.
    define the position<mid_iface> {
        it may only contain particles where {
            it has the position</bar_position>.
        }
    }
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        define the position<bar_holder> {
            it may only contain particles where {
                it has the action<mv:define-lang.org:dep_lib:/bar>.
            }
        }
        create a particle in position<bar_holder>.
        move the particle in position<mid_iface>::position</bar_position> to position<bar_holder>::action<mv:define-lang.org:dep_lib:/bar>::position<item>.
        create a particle in position<bar_holder>::action<mv:define-lang.org:dep_lib:/bar>::position<trigger_pos>.
    }
}
