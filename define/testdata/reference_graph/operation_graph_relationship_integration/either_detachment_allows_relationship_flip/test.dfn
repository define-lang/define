# Either detachment permits the reversal; requiring both loses safe concurrency.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<parent> {
            it may only contain particles where {
                it has the position</child>.
            }
        }
        define the position<detached_child> {
            it may only contain particles where {
                it has the position</inner>.
            }
        }
        define the position<detached_grandchild> {
            it may only contain particles where {
                it has the position</end>.
            }
        }
        create a particle in position<parent>.
        create a particle in position<parent>::position</child>.
        create a particle in position<parent>::position</child>::position</inner>.
        move the particle in position<parent>::position</child> to position<detached_child>.
        move the particle in position<detached_child>::position</inner> to position<detached_grandchild>.
        move the particle in position<parent> to position<detached_grandchild>::position</end>.
    }
}
