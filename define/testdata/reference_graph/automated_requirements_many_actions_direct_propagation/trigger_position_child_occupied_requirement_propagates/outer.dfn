define the potential action<my.domain.com:my_lib:/outer> {
    define the position<trigger_pos>.
    define the position<iface> {
        it may only contain particles where {
            it has the action</inner>.
        }
    }
    define the position<source> {
        it may only contain particles where {
            it has the position</x>.
        }
    }
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        # Here we fail to fill inner's trigger_pos::/x.
        move the particle in position<source> to position<iface>::action</inner>::position<trigger_pos>.
        destroy the particle in position<iface>.
        destroy the particle in position<trigger_pos>.
    }
}
