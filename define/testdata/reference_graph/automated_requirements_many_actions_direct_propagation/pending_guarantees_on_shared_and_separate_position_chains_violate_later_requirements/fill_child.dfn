define the potential action<my.domain.com:my_lib:/fill_child> {
    it also assigns the position</child_marker>.
    define the position<trigger_pos>.
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        create a particle in position</child_marker>.
    }
}
