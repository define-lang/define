define the potential action<mv:define-lang.org:test_init_requires_occupied:/p> {
    it also assigns the position</q>.
    it happens when {
        this particle is created.
    } and it does {
        destroy the particle in position</q>.
    }
}
