define the potential action<my.domain.com:my_lib:/destroy_grandchild> {
    it also assigns the position</q>.
    it happens when {
        this particle is created.
    } and it does {
        destroy the particle in position</q>::position</child>::position</grandchild>.
    }
}
