define the potential action<my.domain.com:my_lib:/destroy_interface_child> {
    it also assigns the position</q>.
    it happens when {
        this particle is created.
    } and it does {
        destroy the particle in position</q>::position</outer>::action</provide_interface>::position<iface>::position</child>.
    }
}
