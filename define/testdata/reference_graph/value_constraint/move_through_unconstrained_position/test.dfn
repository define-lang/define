# An unconstrained position does not remove a particle's value type.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<source> {
            it may only contain particles where {
                it has the value</number>.
            }
        }
        define the position<temporary>.
        define the position<target> {
            it may only contain particles where {
                it has the value</number>.
            }
        }
        create a particle in position<source>.
        move the particle in position<source> to position<temporary>.
        move the particle in position<temporary> to position<target>.
        destroy the particle in position<target>.
    }
}
