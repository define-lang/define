define the potential action<my.domain.com:my_lib:/outer> {
    define the position<trigger_pos>.
    define the position<iface> {
        it may only contain particles where {
            it has the position</intermediate>.
        }
    }
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        define the position<local> {
            it may only contain particles where {
                it has the position</child>.
            }
        }
        move the particle in position<iface>::position</intermediate> to position<local>.
        define the position<inner_holder> {
            it may only contain particles where {
                it has the action</inner>.
            }
        }
        create a particle in position<inner_holder>.
        move the particle in position<local>::position</child> to position<inner_holder>::action</inner>::position<input>.
        create a particle in position<inner_holder>::action</inner>::position<trigger_pos>.
    }
}
