define the potential action<my.domain.com:my_lib:/test> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<source> {
            it may only contain particles where {
                it has the action</parent>.
            }
        }
        define the position<box> {
            it may only contain particles where {
                it has the action</parent>.
            }
        }
        create a particle in position<source>.
        create a particle in position<source>::action</parent>::position<iface>.
        create a particle in position<source>::action</parent>::position<iface>::position</branch>.
        move the particle in position<source> to position<box>.
        create a particle in position<box>::action</parent>::position<iface>::position</branch>::action</child>::position<run>.
        create a particle in position<box>::action</parent>::position<run>.
    }
}
