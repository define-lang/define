define the potential action<my.domain.com:my_lib:/parent> {
    define the position<run>.
    define the position<iface> {
        it may only contain particles where {
            it has the position</branch>.
        }
    }
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in position<iface>::position</branch>::action</child>::position<result>.
        create a particle in position<iface>::position</branch>::action</child>::position<run>.
        destroy the particle in position<iface>.
        destroy the particle in position<run>.
    }
}
