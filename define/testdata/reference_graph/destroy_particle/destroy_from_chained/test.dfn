define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</act>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</act>::position<chain_src>.
        create a particle in action</act>::position<chain_src>::position</mid>.
        create a particle in action</act>::position<chain_src>::position</mid>::position</end>.
        create a particle in action</act>::position<trigger>.
    }
}
