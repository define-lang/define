define the potential action<mv:define-lang.org:parent:/test> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<from_pos>.
        define the position<dest> {
            it may only contain particles where {
                it has the position<mv:define-lang.org:child:/x>.
            }
        }
        create a particle in position<from_pos>.
        create a particle in position<dest>.
        move the particle in position<from_pos> to position<dest>::position<mv:define-lang.org:child:/x>.
    }
}
