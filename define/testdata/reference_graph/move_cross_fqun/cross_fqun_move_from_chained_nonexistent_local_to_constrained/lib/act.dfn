define the potential action<mv:define-lang.org:child:/act> {
    define the position<trigger>.
    define the position<inner>.
    it happens when {
        the position<trigger> has a particle.
    } and it does {
        create a particle in position<inner>.
        destroy the particle in position<inner>.
        destroy the particle in position<trigger>.
    }
}
