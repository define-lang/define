define the potential action<mv:define-lang.org:test_pos_init_chain_bad:/test> {
    it also assigns the position</other>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in position</other>::position</wrong>.
    }
}
