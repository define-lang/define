define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</middle_a>.
    it also assigns the action</middle_b>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</middle_a>::position<run>.
        create a particle in action</middle_b>::position<run>.
    }
}
