"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal


class Grind(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.InterfacePosition("position<beans>"),
                literal.InterfacePosition("position<grounds>"),
            ],
            trigger_position_name="position<beans>",
        )

    @override
    def execute(self):
        self.get_interface_position(
            "position<beans>"
        ).move_particle_to(
            self.get_interface_position(
                "position<grounds>"
            )
        )
