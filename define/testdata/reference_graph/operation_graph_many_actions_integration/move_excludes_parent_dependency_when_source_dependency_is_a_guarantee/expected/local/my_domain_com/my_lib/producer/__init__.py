"""Generated by the Define compiler."""

from typing import ClassVar, override

from define.runtime import literal


class Producer(literal.Action):
    typed_name: ClassVar[str] = "action<my.domain.com:my_lib:/producer>"

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.InterfacePosition("position<input>"),
                literal.InterfacePosition("position<result>"),
            ],
            trigger_position_name="position<input>",
        )

    @override
    def execute(self):
        self.get_interface_position(
            "position<input>"
        ).move_particle_to(
            self.get_interface_position(
                "position<result>"
            )
        )
