define the potential action<my.domain.com:my_lib:/destroyer> {
    define the position<run> {
        it may only contain particles where {
            it has the position</child>.
        }
    }
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<keeper>.
        move the particle in position<run>::position</child> to position<keeper>.
        move the particle in position<keeper> to position<run>::position</child>.
        destroy the particle in position<run>.
    }
}
