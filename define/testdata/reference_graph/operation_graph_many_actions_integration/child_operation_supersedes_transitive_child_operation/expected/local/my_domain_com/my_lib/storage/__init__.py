"""Generated by the Define compiler."""

from typing import final

from define.runtime import literal


class Storage(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<trigger_pos>",
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class StorageExecution:
    def __init__(
        self,
        scheduler: literal.Scheduler,
    ):
        self.scheduler = scheduler
        self.local_position_local_item = literal.LocalPosition(
            "position<local_item>",
            scheduler=self.scheduler,
        )

    def on_action_parent_occupied(self):
        self.create_position_local_item()

    def create_position_local_item(self):
        self.local_position_local_item.create_particle()
        self.local_position_local_item.destroy_particle()
