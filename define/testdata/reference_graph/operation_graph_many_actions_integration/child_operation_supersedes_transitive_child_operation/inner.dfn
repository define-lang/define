define the potential action<my.domain.com:my_lib:/inner> {
    it also assigns the action</storage>.
    define the position<trigger_pos>.
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        create a particle in action</storage>::position<trigger_pos>.
        destroy the particle in action</storage>::position<trigger_pos>.
    }
}
