define the potential action<my.domain.com:my_lib:/consumer> {
    it also assigns the position</box>.
    define the position<trigger_pos>.
    it happens when {
        the position<trigger_pos> has a particle.
    } and it does {
        create a particle in position</box>::position</item>.
        move the particle in position</box>::position</item> to position</box>::position</destination>.
    }
}
