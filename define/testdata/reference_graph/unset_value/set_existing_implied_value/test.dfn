# Setting an existing implied value satisfies a reader and guarantees a set value to the caller.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<worker> {
            it may only contain particles where {
                it has the action</initialize>.
                it has the position</value>.
            }
        }
        create a particle in position<worker>.
        create a particle in position<worker>::position</value>.
        create a particle in position<worker>::action</initialize>::position<trigger>.
        define the position<copy> {
            it may only contain particles where {
                it has the value<standard:/number/rational>.
            }
        }
        create a particle in position<copy>.
        set the value of position<copy> to position<worker>::position</value>.
    }
}
