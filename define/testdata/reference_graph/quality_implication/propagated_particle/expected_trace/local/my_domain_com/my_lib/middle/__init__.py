"""Generated by the Define compiler."""

from typing import override

from define.runtime import literal

import local.my_domain_com.my_lib.implied
import local.my_domain_com.my_lib.implier
import local.my_domain_com.my_lib.inner


class MiddleDestructionContracts:
    def run_destructors_position_box__position_implied(self, _particle: literal.Particle):
        pass

    def destroy_position_box__position_implied(self, _particle: literal.Particle):
        pass

    def run_destructors_position_box(self, _particle: literal.Particle):
        pass

    def destroy_position_box(self, _particle: literal.Particle):
        pass

    def run_destructors_position_run(self, _particle: literal.Particle):
        pass

    def destroy_position_run(self, _particle: literal.Particle):
        pass


_DEFAULT_DESTRUCTION_CONTRACTS = MiddleDestructionContracts()


class Middle(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition("position<run>"),
                literal.LocalPosition(
                    "position<box>",
                    constraints=(
                        local.my_domain_com.my_lib.implier.Implier,
                    ),
                ),
                literal.LocalPosition(
                    "position<final>",
                    constraints=(
                        local.my_domain_com.my_lib.implied.Implied,
                    ),
                ),
            ],
        )

    @override
    def run(self, destruction_contracts: MiddleDestructionContracts = _DEFAULT_DESTRUCTION_CONTRACTS):
        inner_holder = literal.LocalPosition(
            "position<inner_holder>",
            constraints=(
                local.my_domain_com.my_lib.inner.Inner,
            ),
        )
        inner_holder.create_particle()
        literal.record_operation("middle.create(inner_holder)")
        self.get_interface_position(
            "position<box>"
        ).move_particle_to(
            inner_holder.particle.get_action(
                local.my_domain_com.my_lib.inner.Inner
            ).get_interface_position(
                "position<input>"
            )
        )
        literal.record_operation("middle.move(box, inner_holder::/inner::input)")
        inner_holder.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<run>"
        ).create_particle()
        literal.record_operation("middle.create(inner_holder::/inner::run)")
        inner_holder.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).run()
        inner_holder.particle.get_action(
            local.my_domain_com.my_lib.inner.Inner
        ).get_interface_position(
            "position<output>"
        ).move_particle_to(
            self.get_interface_position(
                "position<final>"
            )
        )
        literal.record_operation("middle.move(inner_holder::/inner::output, final)")
        destruction_contracts.run_destructors_position_box__position_implied(
            self.get_interface_position(
                "position<final>"
            ).particle.get_position(
                local.my_domain_com.my_lib.implied.Implied
            ).particle
        )
        destruction_contracts.destroy_position_box__position_implied(
            self.get_interface_position(
                "position<final>"
            ).particle.get_position(
                local.my_domain_com.my_lib.implied.Implied
            ).particle
        )
        self.get_interface_position(
            "position<final>"
        ).particle.get_position(
            local.my_domain_com.my_lib.implied.Implied
        ).destroy_particle()
        literal.record_operation("middle.destroy(final::/implied)")
        destruction_contracts.run_destructors_position_box(
            self.get_interface_position(
                "position<final>"
            ).particle
        )
        destruction_contracts.destroy_position_box(
            self.get_interface_position(
                "position<final>"
            ).particle
        )
        self.get_interface_position(
            "position<final>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(final)")
        destruction_contracts.run_destructors_position_run(
            self.get_interface_position(
                "position<run>"
            ).particle
        )
        destruction_contracts.destroy_position_run(
            self.get_interface_position(
                "position<run>"
            ).particle
        )
        self.get_interface_position(
            "position<run>"
        ).destroy_particle()
        literal.record_operation("middle.destroy(run)")
        inner_holder.destroy_particle()
        literal.record_operation("middle.destroy(inner_holder)")
