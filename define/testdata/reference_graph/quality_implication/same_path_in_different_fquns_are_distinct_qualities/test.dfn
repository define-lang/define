define the potential action<mv:define-lang.org:cross_implication_main:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<source> {
            it may only contain particles where {
                it has the action</implier>.
            }
        }
        define the position<dest> {
            it may only contain particles where {
                it has the position<mv:define-lang.org:cross_implication_lib_b:/foo>.
            }
        }
        create a particle in position<source>.
        move the particle in position<source> to position<dest>.
        create a particle in position<dest>::position<mv:define-lang.org:cross_implication_lib_b:/foo>.
    }
}
