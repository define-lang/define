define the potential action<my.domain.com:my_lib:/test> {
    define the position<box> {
        it may only contain particles where {
            it has the action</worker>.
        }
    }
    it happens when {
        this particle is created.
    } and it does {
        define the position<feeder>.
        define the position<sink>.
        create a particle in position<box>.
        create a particle in position<feeder>.
        move the particle in position<feeder> to position<box>::action</worker>::position<input>.
        move the particle in position<box>::action</worker>::position<input> to position<sink>.
    }
}
