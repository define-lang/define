define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</cleanup>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</cleanup>::position<child>.
    }
}
