define the potential action<mv:define-lang.org:test_init_requires_empty:/filler> {
    it also assigns the position</q>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in position</q>.
    }
}
