# /inner creates in position<item>, requiring it empty.
#
# /outer transfers its input particle to /inner and triggers it. /test pre-fills
# the particle's position</item>, then triggers /outer. The inner requirement
# that position</item> be empty produces an inferred-requirement-violation
# diagnostic.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<local> {
            it may only contain particles where {
                it has the action</outer>.
            }
        }
        create a particle in position<local>.
        create a particle in position<local>::action</outer>::position<outer_iface>.
        create a particle in position<local>::action</outer>::position<outer_iface>::position</item>.
        create a particle in position<local>::action</outer>::position<trigger_pos>.
    }
}
