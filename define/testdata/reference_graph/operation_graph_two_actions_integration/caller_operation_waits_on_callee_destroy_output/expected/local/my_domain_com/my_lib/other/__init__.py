"""Generated by the Define compiler."""

from typing import final

from define.runtime import literal


class Other(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<trigger_pos>",
                    scheduler=on_particle.scheduler,
                ),
                literal.LocalPosition(
                    "position<output>",
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class OtherGuarantees:
    def __init__(self):
        self.position_output = literal.Guarantee()
        self.position_trigger_pos = literal.Guarantee()


@final
class OtherExecution:
    def __init__(
        self,
        action: Other,
        scheduler: literal.Scheduler,
        *,
        destruction_connections: literal.DestructionConnections | None = None,
    ):
        self.action = action
        self.scheduler = scheduler
        self.guarantees = OtherGuarantees()
        self.destruction_connections = destruction_connections
        self.join_for_destroy_position_output: literal.Join
        self.join_for_destroy_position_trigger_pos: literal.Join
        self.join_for_empty_rule_position_output: literal.Join
        self.join_for_empty_rule_position_trigger_pos: literal.Join

    def accept_for_empty_rule_position_output(self):
        if not self.join_for_empty_rule_position_output.arrive():
            return
        self.destroy_position_output()

    def accept_for_empty_rule_position_trigger_pos(self):
        if not self.join_for_empty_rule_position_trigger_pos.arrive():
            return
        self.destroy_position_trigger_pos()

    def destroy_position_output(self):
        if not self.join_for_destroy_position_output.arrive():
            return
        literal.continue_destruction(self.continue_destroy_position_output)

    def continue_destroy_position_output(self):
        self.action.get_interface_position(
            "position<output>"
        ).destroy_particle()
        self.guarantees.position_output.publish(
            self.scheduler,
        )

    def destroy_position_trigger_pos(self):
        if not self.join_for_destroy_position_trigger_pos.arrive():
            return
        literal.continue_destruction(self.continue_destroy_position_trigger_pos)

    def continue_destroy_position_trigger_pos(self):
        self.action.get_interface_position(
            "position<trigger_pos>"
        ).destroy_particle()
        self.guarantees.position_trigger_pos.publish(
            self.scheduler,
        )
