"""Generated by the Define compiler."""

from typing import ClassVar, final

from define.runtime import literal

import local.my_domain_com.my_lib.destination


class Worker(literal.Action):
    implied_qualities: ClassVar[tuple[type[literal.Quality], ...]] = (
        local.my_domain_com.my_lib.destination.Destination,
    )

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<source>",
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class WorkerGuarantees:
    def __init__(self):
        self.position_source = literal.Guarantee()
        self.global_position_destination = literal.Guarantee()


@final
class WorkerExecution:
    def __init__(
        self,
        action: Worker,
        scheduler: literal.Scheduler,
        *,
        destruction_connections: literal.DestructionConnections | None = None,
    ):
        self.action = action
        self.scheduler = scheduler
        self.guarantees = WorkerGuarantees()
        self.destruction_connections = destruction_connections
        self.join_for_move_position_source_to_global_position_destination: literal.Join
        self.join_for_empty_rule_position_source: literal.Join

    def accept_for_empty_rule_position_source(self):
        if not self.join_for_empty_rule_position_source.arrive():
            return
        self.move_position_source_to_global_position_destination()

    def move_position_source_to_global_position_destination(self):
        if not self.join_for_move_position_source_to_global_position_destination.arrive():
            return
        self.action.get_interface_position(
            "position<source>"
        ).move_particle_to(
            self.action.on_particle.get_position(
                local.my_domain_com.my_lib.destination.Destination
            )
        )
        self.guarantees.position_source.publish(
            self.scheduler,
            self.destroy_global_position_destination,
        )

    def destroy_global_position_destination(self):
        literal.continue_destruction(self.continue_destroy_global_position_destination)

    def continue_destroy_global_position_destination(self):
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.destination.Destination
        ).destroy_particle()
        self.guarantees.global_position_destination.publish(
            self.scheduler,
        )
