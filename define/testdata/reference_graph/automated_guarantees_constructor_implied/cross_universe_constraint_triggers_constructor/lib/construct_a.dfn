define the potential action<mv:define-lang.org:child:/construct_a> {
    it also assigns the position</a>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in position</a>.
    }
}
