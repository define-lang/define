# A defined Potential Literal can use the built-in decimal encoding.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<target> {
            it may only contain particles where {
                it has the value<standard:/number/rational>.
            }
        }
        create a particle in position<target>.
        set the value of position<target> to literal</decimal>"-12.50".
        destroy the particle in position<target>.
    }
}
