# The children's verification in /middle must not suppress the parent's in /test.
define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</middle>.
    it happens when {
        this particle is created.
    } and it does {
        define the position<source> {
            it may only contain particles where {
                it has the action</destructor>.
                it has the position</item>.
                it has the position</child>.
                it has the position</other_child>.
            }
        }
        create a particle in position<source>.
        create a particle in position<source>::position</item>.
        move the particle in position<source> to action</middle>::position<run>.
    }
}
