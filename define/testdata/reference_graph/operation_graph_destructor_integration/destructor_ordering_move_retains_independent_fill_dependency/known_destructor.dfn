define the potential action<my.domain.com:my_lib:/known_destructor> {
    it also assigns the position</shared>.
    it happens when {
        this particle is being destroyed.
    } and it does {
        define the position<holder>.
        move the particle in position</shared> to position<holder>.
        move the particle in position<holder> to position</shared>.
    }
}
