# make_thing attaches a destructor in a local position and moves the particle to
# result, whose constraints omit the destructor. The result guarantee propagates
# the destructor to /test, where destroying result triggers it.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<box> {
            it may only contain particles where {
                it has the action</make_thing>.
            }
        }
        create a particle in position<box>.
        create a particle in position<box>::action</make_thing>::position<run>.
        destroy the particle in position<box>::action</make_thing>::position<result>.
    }
}
