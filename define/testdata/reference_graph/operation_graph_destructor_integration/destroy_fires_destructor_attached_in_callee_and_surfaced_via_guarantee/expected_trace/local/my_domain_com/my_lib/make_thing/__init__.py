"""Generated by the Define compiler."""

from typing import final

from define.runtime import literal

import local.my_domain_com.my_lib.destructor


class MakeThing(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<result>",
                    scheduler=on_particle.scheduler,
                ),
                literal.LocalPosition(
                    "position<run>",
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class MakeThingGuarantees:
    def __init__(self):
        self.guarantee_position_result: list[literal.Task] = []


@final
class MakeThingExecution:
    def __init__(
        self,
        action: MakeThing,
        scheduler: literal.Scheduler,
        caller_execution: object | None,
        action_name: str,
        guarantees: MakeThingGuarantees,
    ):
        self.action = action
        self.scheduler = scheduler
        self.trace_execution = scheduler.execution_created(
            caller_execution,
            action_name,
        )
        self.guarantees = guarantees
        self.local_position_temp = literal.LocalPosition(
            "position<temp>",
            constraints=(
                local.my_domain_com.my_lib.destructor.Destructor,
            ),
            scheduler=self.scheduler,
        )
        self.join_for_move_position_temp_to_position_result = literal.Join(2)

    def accept_action_parent(self):
        self.create_position_temp()

    def accept_when_empty_position_result(self):
        self.move_position_temp_to_position_result()

    def create_position_temp(self):
        self.local_position_temp.create_particle()
        self.scheduler.create_completed(
            self.trace_execution,
            "temp",
            1,
        )
        self.move_position_temp_to_position_result()

    def move_position_temp_to_position_result(self):
        if not self.join_for_move_position_temp_to_position_result.arrive():
            return
        self.local_position_temp.move_particle_to(
            self.action.get_interface_position(
                "position<result>"
            )
        )
        self.scheduler.move_completed(
            self.trace_execution,
            "temp",
            "result",
            1,
        )
        self.scheduler.continue_with(self.guarantees.guarantee_position_result)
