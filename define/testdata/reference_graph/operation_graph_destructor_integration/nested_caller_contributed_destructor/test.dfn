define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<outer> {
            it may only contain particles where {
                it has the action</outer_destructor>.
            }
        }
        create a particle in position<outer>.
        destroy the particle in position<outer>.
    }
}
