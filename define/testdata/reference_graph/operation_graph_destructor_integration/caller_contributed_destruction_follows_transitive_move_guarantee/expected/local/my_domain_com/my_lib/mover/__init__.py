"""Generated by the Define compiler."""

from typing import final

from define.runtime import literal


class Mover(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<run>",
                    scheduler=on_particle.scheduler,
                ),
                literal.LocalPosition(
                    "position<result>",
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class MoverGuarantees:
    def __init__(self, scheduler: literal.Scheduler):
        self.position_run__move__position_result = literal.Fanout(scheduler)


@final
class MoverExecution:
    def __init__(
        self,
        action: Mover,
        scheduler: literal.Scheduler,
    ):
        self.action = action
        self.scheduler = scheduler
        self.guarantees = MoverGuarantees(self.scheduler)
        self.join_for_move_position_run_to_position_result: literal.Join
        self.join_for_empty_rule_position_run: literal.Join

    def accept_for_empty_rule_position_run(self):
        if not self.join_for_empty_rule_position_run.arrive():
            return
        self.move_position_run_to_position_result()

    def move_position_run_to_position_result(self):
        if not self.join_for_move_position_run_to_position_result.arrive():
            return
        self.action.get_interface_position(
            "position<run>"
        ).move_particle_to(
            self.action.get_interface_position(
                "position<result>"
            )
        )
        self.guarantees.position_run__move__position_result.run(
        )
