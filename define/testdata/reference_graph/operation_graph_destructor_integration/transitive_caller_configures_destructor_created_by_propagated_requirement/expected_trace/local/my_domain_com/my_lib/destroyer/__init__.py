"""Generated by the Define compiler."""

from typing import ClassVar, final

from define.runtime import literal

import local.my_domain_com.my_lib.known_destructor
import local.my_domain_com.my_lib.target


class Destroyer(literal.Action):
    implied_qualities: ClassVar[tuple[type[literal.Quality], ...]] = (
        local.my_domain_com.my_lib.target.Target,
    )

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<trigger_pos>",
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class DestroyerGuarantees:
    def __init__(self):
        self.global_position_target = literal.Fanout()


@final
class DestroyerExecution:
    def __init__(
        self,
        action: Destroyer,
        scheduler: literal.Scheduler,
        caller_execution: object | None,
        action_name: str,
        *,
        destruction_connections: literal.DestructionConnections | None = None,
    ):
        self.action = action
        self.scheduler = scheduler
        self.trace_execution = scheduler.execution_created(
            caller_execution,
            action_name,
        )
        self.guarantees = DestroyerGuarantees()
        self.destruction_connections = destruction_connections
        self.execution_global_position_target__action_known_destructor: local.my_domain_com.my_lib.known_destructor.KnownDestructorExecution
        self.join_for_destroy_global_position_target: literal.Join
        self.join_for_empty_rule_global_position_target: literal.Join

    def accept_when_empty_global_position_target__global_position_marker(self):
        self.execution_global_position_target__action_known_destructor.accept_when_empty_global_position_marker()

    def accept_for_empty_rule_global_position_target(self):
        if not self.join_for_empty_rule_global_position_target.arrive():
            return
        self.destroy_global_position_target()

    def init_global_position_target__action_known_destructor(self):
        self.execution_global_position_target__action_known_destructor = local.my_domain_com.my_lib.known_destructor.KnownDestructorExecution(
            self.action.on_particle.get_position(
                local.my_domain_com.my_lib.target.Target
            ).particle.get_action(
                local.my_domain_com.my_lib.known_destructor.KnownDestructor
            ),
            self.scheduler,
            self.trace_execution,
            "known_destructor",
        )
        self.execution_global_position_target__action_known_destructor.guarantees.global_position_marker.consumers.append(
            self.destroy_global_position_target
        )

    def destroy_global_position_target(self):
        if not self.join_for_destroy_global_position_target.arrive():
            return
        literal.continue_destruction(self.continue_destroy_global_position_target)

    def continue_destroy_global_position_target(self):
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.target.Target
        ).destroy_particle()
        self.scheduler.destroy_completed(
            self.trace_execution,
            "/target",
            1,
        )
        self.guarantees.global_position_target.publish(
            self.scheduler,
        )
