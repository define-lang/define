"""Generated by the Define compiler."""

from typing import ClassVar, final

from define.runtime import literal

import local.my_domain_com.my_lib.empty
import local.my_domain_com.my_lib.occupied_first
import local.my_domain_com.my_lib.occupied_last
import local.my_domain_com.my_lib.transitive


class Destructor(literal.Action):
    implied_qualities: ClassVar[tuple[type[literal.Quality], ...]] = (
        local.my_domain_com.my_lib.occupied_first.OccupiedFirst,
        local.my_domain_com.my_lib.empty.Empty,
        local.my_domain_com.my_lib.occupied_last.OccupiedLast,
    )


@final
class DestructorGuarantees:
    def __init__(self):
        self.global_position_occupied_first = literal.Guarantee()
        self.global_position_occupied_first__global_position_transitive = literal.Guarantee()
        self.global_position_empty = literal.Guarantee()
        self.global_position_occupied_last = literal.Guarantee()


@final
class DestructorExecution:
    def __init__(
        self,
        action: Destructor,
        scheduler: literal.Scheduler,
        caller_execution: object | None,
        action_name: str,
    ):
        self.action = action
        self.scheduler = scheduler
        self.trace_execution = scheduler.execution_created(
            caller_execution,
            action_name,
        )
        self.guarantees = DestructorGuarantees()
        self.local_position_first_holder = literal.LocalPosition(
            "position<first_holder>",
            scheduler=self.scheduler,
        )
        self.local_position_transitive_holder = literal.LocalPosition(
            "position<transitive_holder>",
            scheduler=self.scheduler,
        )
        self.local_position_last_holder = literal.LocalPosition(
            "position<last_holder>",
            scheduler=self.scheduler,
        )
        self.join_for_move_global_position_occupied_first_to_position_first_holder: literal.Join
        self.join_for_move_global_position_occupied_last_to_position_last_holder: literal.Join
        self.join_for_empty_rule_global_position_occupied_first: literal.Join
        self.join_for_empty_rule_global_position_occupied_last: literal.Join

    def accept_for_empty_rule_global_position_occupied_first(self):
        if not self.join_for_empty_rule_global_position_occupied_first.arrive():
            return
        self.move_global_position_occupied_first_to_position_first_holder()

    def accept_when_empty_global_position_empty(self):
        self.create_global_position_empty()

    def accept_for_empty_rule_global_position_occupied_last(self):
        if not self.join_for_empty_rule_global_position_occupied_last.arrive():
            return
        self.move_global_position_occupied_last_to_position_last_holder()

    def move_global_position_occupied_first_to_position_first_holder(self):
        if not self.join_for_move_global_position_occupied_first_to_position_first_holder.arrive():
            return
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.occupied_first.OccupiedFirst
        ).move_particle_to(self.local_position_first_holder)
        self.scheduler.move_completed(
            self.trace_execution,
            "/occupied_first",
            "first_holder",
            1,
        )
        self.local_position_first_holder.move_particle_to(
            self.action.on_particle.get_position(
                local.my_domain_com.my_lib.occupied_first.OccupiedFirst
            )
        )
        self.scheduler.move_completed(
            self.trace_execution,
            "first_holder",
            "/occupied_first",
            1,
        )
        self.guarantees.global_position_occupied_first.publish(
            self.scheduler,
            self.move_global_position_occupied_first__global_position_transitive_to_position_transitive_holder,
        )

    def move_global_position_occupied_first__global_position_transitive_to_position_transitive_holder(self):
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.occupied_first.OccupiedFirst
        ).particle.get_position(
            local.my_domain_com.my_lib.transitive.Transitive
        ).move_particle_to(self.local_position_transitive_holder)
        self.scheduler.move_completed(
            self.trace_execution,
            "/occupied_first::/transitive",
            "transitive_holder",
            1,
        )
        self.local_position_transitive_holder.move_particle_to(
            self.action.on_particle.get_position(
                local.my_domain_com.my_lib.occupied_first.OccupiedFirst
            ).particle.get_position(
                local.my_domain_com.my_lib.transitive.Transitive
            )
        )
        self.scheduler.move_completed(
            self.trace_execution,
            "transitive_holder",
            "/occupied_first::/transitive",
            1,
        )
        self.guarantees.global_position_occupied_first__global_position_transitive.publish(
            self.scheduler,
        )

    def create_global_position_empty(self):
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.empty.Empty
        ).create_particle()
        self.scheduler.create_completed(
            self.trace_execution,
            "/empty",
            1,
        )
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.empty.Empty
        ).destroy_particle()
        self.scheduler.destroy_completed(
            self.trace_execution,
            "/empty",
            1,
        )
        self.guarantees.global_position_empty.publish(
            self.scheduler,
        )

    def move_global_position_occupied_last_to_position_last_holder(self):
        if not self.join_for_move_global_position_occupied_last_to_position_last_holder.arrive():
            return
        self.action.on_particle.get_position(
            local.my_domain_com.my_lib.occupied_last.OccupiedLast
        ).move_particle_to(self.local_position_last_holder)
        self.scheduler.move_completed(
            self.trace_execution,
            "/occupied_last",
            "last_holder",
            1,
        )
        self.local_position_last_holder.move_particle_to(
            self.action.on_particle.get_position(
                local.my_domain_com.my_lib.occupied_last.OccupiedLast
            )
        )
        self.scheduler.move_completed(
            self.trace_execution,
            "last_holder",
            "/occupied_last",
            1,
        )
        self.guarantees.global_position_occupied_last.publish(
            self.scheduler,
        )
