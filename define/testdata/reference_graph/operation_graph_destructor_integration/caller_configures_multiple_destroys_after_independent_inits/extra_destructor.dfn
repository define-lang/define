define the potential action<my.domain.com:my_lib:/extra_destructor> {
    it also assigns the position</marker>.
    it also assigns the position</empty_marker>.
    it happens when {
        this particle is being destroyed.
    } and it does {
        move the particle in position</marker> to position</empty_marker>.
        move the particle in position</empty_marker> to position</marker>.
    }
}
