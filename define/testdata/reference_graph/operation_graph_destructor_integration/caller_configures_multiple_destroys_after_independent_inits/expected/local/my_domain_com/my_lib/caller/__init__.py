"""Generated by the Define compiler."""

from typing import ClassVar, final

from define.runtime import literal

import local.my_domain_com.my_lib.extra_destructor
import local.my_domain_com.my_lib.known_destructor
import local.my_domain_com.my_lib.middle


class Caller(literal.Action):
    implied_qualities: ClassVar[tuple[type[literal.Quality], ...]] = (
        local.my_domain_com.my_lib.middle.Middle,
    )

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<run>",
                    constraints=(
                        local.my_domain_com.my_lib.extra_destructor.ExtraDestructor,
                        local.my_domain_com.my_lib.known_destructor.KnownDestructor,
                    ),
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class CallerGuarantees:
    def __init__(self):
        self.position_run = literal.Fanout()


@final
class CallerExecution:
    def __init__(
        self,
        action: Caller,
        scheduler: literal.Scheduler,
        *,
        destruction_connections: literal.DestructionConnections | None = None,
    ):
        self.action = action
        self.scheduler = scheduler
        self.guarantees = CallerGuarantees()
        self.destruction_connections = destruction_connections
        self.execution_action_middle: local.my_domain_com.my_lib.middle.MiddleExecution
        self.join_for_move_position_run_to_action_middle__position_run: literal.Join
        self.join_for_empty_rule_position_run: literal.Join
        self.join_when_occupied_global_position_parent: literal.Join
        self.join_for_empty_rule_global_position_parent: literal.Join
        self.execution_action_middle = local.my_domain_com.my_lib.middle.MiddleExecution(
            self.action.on_particle.get_action(
                local.my_domain_com.my_lib.middle.Middle
            ),
            self.scheduler,
            destruction_connections=self.destruction_connections,
        )
        self.execution_action_middle.join_for_move_position_run_to_global_position_parent__action_destroyer__position_target = literal.NO_JOIN

    def accept_for_empty_rule_position_run(self):
        if not self.join_for_empty_rule_position_run.arrive():
            return
        self.move_position_run_to_action_middle__position_run()

    def accept_when_occupied_global_position_parent(self):
        if not self.join_when_occupied_global_position_parent.arrive():
            return
        self.execution_action_middle.accept_for_empty_rule_position_run()

    def accept_when_occupied_global_position_parent_2(self):
        self.execution_action_middle.accept_when_occupied_global_position_parent()

    def accept_for_empty_rule_global_position_parent(self):
        if not self.join_for_empty_rule_global_position_parent.arrive():
            return
        self.execution_action_middle.accept_for_empty_rule_global_position_parent()

    def move_position_run_to_action_middle__position_run(self):
        if not self.join_for_move_position_run_to_action_middle__position_run.arrive():
            return
        self.action.get_interface_position(
            "position<run>"
        ).move_particle_to(
            self.action.on_particle.get_action(
                local.my_domain_com.my_lib.middle.Middle
            ).get_interface_position(
                "position<run>"
            )
        )
        self.guarantees.position_run.run(
            self.scheduler,
            self.execution_action_middle.accept_for_empty_rule_position_run,
        )
