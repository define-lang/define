"""Generated by the Define compiler."""

from typing import final

from define.runtime import literal

import local.my_domain_com.my_lib.child
import local.my_domain_com.my_lib.child_destructor


class Worker(literal.Action):

    def __init__(self, on_particle: literal.Particle):
        super().__init__(
            on_particle,
            interface_positions=[
                literal.LocalPosition(
                    "position<target>",
                    constraints=(
                        local.my_domain_com.my_lib.child.Child,
                    ),
                    scheduler=on_particle.scheduler,
                ),
            ],
        )


@final
class WorkerGuarantees:
    def __init__(self, scheduler: literal.Scheduler):
        self.position_target = literal.Fanout(scheduler)


@final
class WorkerExecution:
    def __init__(
        self,
        action: Worker,
        scheduler: literal.Scheduler,
        caller_execution: object | None,
        action_name: str,
        *,
        destruction_connections: literal.DestructionConnections | None = None,
    ):
        self.action = action
        self.scheduler = scheduler
        self.trace_execution = scheduler.execution_created(
            caller_execution,
            action_name,
        )
        self.guarantees = WorkerGuarantees(self.scheduler)
        self.destruction_connections = destruction_connections
        self.execution_position_target__global_position_child__action_child_destructor: local.my_domain_com.my_lib.child_destructor.ChildDestructorExecution
        self.destruction_position_position_target__global_position_child: literal.Position
        self.join_for_destroy_position_target: literal.Join
        self.join_for_empty_rule_position_target: literal.Join

    def accept_when_empty_position_target__global_position_child(self):
        self.create_position_target__global_position_child()

    def accept_for_empty_rule_position_target(self):
        if not self.join_for_empty_rule_position_target.arrive():
            return
        self.destroy_position_target()

    def create_position_target__global_position_child(self):
        self.action.get_interface_position(
            "position<target>"
        ).particle.get_position(
            local.my_domain_com.my_lib.child.Child
        ).create_particle()
        self.scheduler.create_completed(
            self.trace_execution,
            "target::/child",
            1,
        )
        self.destruction_position_position_target__global_position_child = self.action.get_interface_position(
            "position<target>"
        ).particle.get_position(
            local.my_domain_com.my_lib.child.Child
        )
        self.execution_position_target__global_position_child__action_child_destructor = local.my_domain_com.my_lib.child_destructor.ChildDestructorExecution(
            self.scheduler,
            self.trace_execution,
            "child_destructor",
        )
        self.scheduler.continue_with(
            self.destroy_position_target,
            self.destroy_position_target__global_position_child,
            self.execution_position_target__global_position_child__action_child_destructor.on_action_parent_occupied,
        )

    def destroy_position_target(self):
        if not self.join_for_destroy_position_target.arrive():
            return
        literal.continue_destruction(self.continue_destroy_position_target)

    def continue_destroy_position_target(self):
        self.action.get_interface_position(
            "position<target>"
        ).destroy_particle()
        self.scheduler.destroy_completed(
            self.trace_execution,
            "target",
            1,
        )
        self.guarantees.position_target.run()

    def destroy_position_target__global_position_child(self):
        literal.continue_destruction(self.continue_destroy_position_target__global_position_child)

    def continue_destroy_position_target__global_position_child(self):
        self.destruction_position_position_target__global_position_child.destroy_particle()
        self.scheduler.destroy_completed(
            self.trace_execution,
            "target::/child",
            1,
        )
