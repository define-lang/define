define the potential action<my.domain.com:my_lib:/nested_worker> {
    define the position<target>.
    it happens when {
        the position<target> has a particle.
    } and it does {
        destroy the particle in position<target>.
    }
}
