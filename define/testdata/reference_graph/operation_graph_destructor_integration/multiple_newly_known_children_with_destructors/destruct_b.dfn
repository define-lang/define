define the potential action<my.domain.com:my_lib:/destruct_b> {
    it happens when {
        this particle is being destroyed.
    } and it does {
        define the position<_noop_b>.
        create a particle in position<_noop_b>.
    }
}
