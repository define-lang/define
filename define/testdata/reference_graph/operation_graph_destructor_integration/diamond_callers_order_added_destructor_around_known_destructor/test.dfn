define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</caller_a>.
    it also assigns the action</caller_b>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</caller_a>::position<trigger_pos>.
        create a particle in action</caller_b>::position<trigger_pos>.
        destroy the particle in action</caller_a>::position<trigger_pos>.
        destroy the particle in action</caller_b>::position<trigger_pos>.
    }
}
