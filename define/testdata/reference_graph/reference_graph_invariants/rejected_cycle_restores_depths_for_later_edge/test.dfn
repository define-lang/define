# The second reference from /b is another cycle only when the first rejection restores every depth.
define the potential position<mv:define-lang.org:reference_graph_rollback:/test> {
    it may only contain particles where {
        it has the position</c>.
    }
}
