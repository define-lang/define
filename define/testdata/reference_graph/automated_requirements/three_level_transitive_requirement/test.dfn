define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</outer>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</outer>::position<middle_iface>.
        create a particle in action</outer>::position<middle_iface>::action</middle>::position<inner_iface>.
        create a particle in action</outer>::position<run>.
    }
}
