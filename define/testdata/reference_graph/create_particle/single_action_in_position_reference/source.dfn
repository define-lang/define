define the potential action<mv:define-lang.org:test_files:/invalid/particles/create/single_action_reference> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in action<act_other>.
    }
}
