define the potential action<mv:define-lang.org:test_files:/invalid/particles/create/undefined_local_position> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in position<no_such_pos>.
    }
}
