define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</triggered>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</triggered>::position<input>.
        create a particle in action</triggered>::position<input>::position</child>.
        create a particle in action</triggered>::position<run>.
        destroy the particle in action</triggered>::position<input>.
    }
}
