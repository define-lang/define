# /inner moves a particle from its implied action's interface to its own guarantee,
# and /test sees that guarantee occupied.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<box> {
            it may only contain particles where {
                it has the action</inner>.
            }
        }
        create a particle in position<box>.
        create a particle in position<box>::action</inner>::position<trigger_pos>.
        create a particle in position<box>::action</inner>::position<result>.
    }
}
