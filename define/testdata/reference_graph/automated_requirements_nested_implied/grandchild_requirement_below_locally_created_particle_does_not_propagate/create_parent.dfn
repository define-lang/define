define the potential action<my.domain.com:my_lib:/create_parent> {
    it also assigns the position</parent>.
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in position</parent>.
        create a particle in position</parent>::action</inspect_grandchild>::position<child>.
        create a particle in position</parent>::action</inspect_grandchild>::position<run>.
    }
}
