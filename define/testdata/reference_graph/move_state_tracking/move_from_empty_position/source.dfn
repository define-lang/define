define the potential action<mv:define-lang.org:test_files:/invalid/particles/move/from_empty_position> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        define the position<from_pos>.
        define the position<to_pos>.
        move the particle in position<from_pos> to position<to_pos>.
    }
}
