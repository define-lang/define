define the potential action<mv:define-lang.org:test_move_bad_action:/act_b> {
    define the position<inner>.
    it happens when {
        the position<inner> has a particle.
    } and it does {
        define the position<_noop>.
        create a particle in position<_noop>.
        destroy the particle in position<inner>.
    }
}
