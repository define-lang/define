define the potential action<mv:define-lang.org:test_files:/invalid/references/unknown_global_name> {
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in position</valid/minimal_position>.
    }
}
