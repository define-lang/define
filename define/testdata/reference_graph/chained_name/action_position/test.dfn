define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</act>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</act>::position<trigger_pos>.
        create a particle in action</act>::position<trigger_pos>::position</inner>.
        create a particle in action</act>::position<run>.
    }
}
