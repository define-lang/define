define the potential action<my.domain.com:my_library_collection:/test> {
    it also assigns the action</perform_operation>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</perform_operation>::position<operation_trigger>.
        create a particle in action</perform_operation>::position<operation_trigger>::position</inner_position>.
        create a particle in action</perform_operation>::position<run>.
    }
}
