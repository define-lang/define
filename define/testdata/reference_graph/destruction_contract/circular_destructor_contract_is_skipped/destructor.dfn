define the potential action<my.domain.com:my_lib:/destructor> {
    define the position<dependency> {
        it may only contain particles where {
            it has the action</close_file>.
        }
    }
    it happens when {
        this particle is being destroyed.
    } and it does {
        create a particle in position<dependency>.
        destroy the particle in position<dependency>.
    }
}
