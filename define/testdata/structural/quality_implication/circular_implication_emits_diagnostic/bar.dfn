define the potential action<my.domain.com:my_lib:/bar> {
    it also assigns the action</test>.
    define the position<run>.
    it happens when {
        the position<run> has a particle.
    } and it does {
        create a particle in action</test>::position<run>.
    }
}
