# References are discovered serially so the independent delay chain reaches source after the shared target's two paths exist.
define the potential position<mv:define-lang.org:coverage_depth:/test> {
    it may only contain particles where {
        it has the position</root>.
        it has the position</delay_1>.
    }
}
