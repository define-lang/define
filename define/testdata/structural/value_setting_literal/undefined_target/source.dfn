# A valid literal does not suppress target position validation.
define the encoding<my.domain.com:my_lib:/test>.
define the potential literal<my.domain.com:my_lib:/test> {
    it has the encoding</test>.
}
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        set the value of position<missing> to literal</test>"hello".
    }
}
