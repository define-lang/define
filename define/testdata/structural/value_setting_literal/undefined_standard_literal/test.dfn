# Standard literals other than the built-in ones need a configured standard universe.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<dest>.
        set the value of position<dest> to literal<standard:/text>"hello".
    }
}
