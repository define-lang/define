# Values may be referenced from configured dependency universes.
define the potential position<my.domain.com:my_lib:/test> {
    it may only contain particles where {
        it has the value<other.org:other_lib:/number>.
    }
}
