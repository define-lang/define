define the potential action<my.domain.com:my_lib:/first> {
    define the position<input>.
    define the position<other>.
    it happens when {
        this particle is created.
    } and it does {
        destroy the particle in position<input>.
        destroy the particle in position<other>.
    }
}
define the potential action<my.domain.com:my_lib:/last> {
    define the position<input>.
    define the position<other>.
    it happens when {
        this particle is created.
    } and it does {
        destroy the particle in position<input>.
        destroy the particle in position<other>.
    }
}
define the potential position<my.domain.com:my_lib:/later>.
