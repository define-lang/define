# The built-in standard encoding needs no definition file or project configuration.
define the potential literal<my.domain.com:my_lib:/decimal> {
    it has the encoding<standard:/number/decimal/ascii>.
}
