# Duplicate literal.
define the encoding<my.domain.com:my_lib:/decimal_text>.
define the potential literal<my.domain.com:my_lib:/decimal> {
    it has the encoding</decimal_text>.
}
define the potential literal<my.domain.com:my_lib:/decimal> {
    it has the encoding</decimal_text>.
}
