# Global names in both value setting references must load their definition files.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<item>.
        set the value of position<item>::position</target> to position<item>::position</original>.
    }
}
