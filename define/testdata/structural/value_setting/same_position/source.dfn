# A value setting statement cannot use the same position on both sides.
define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<item>.
        set the value of position<item> to position<item>.
    }
}
