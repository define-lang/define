define the potential action<mv:define-lang.org:test_walk_scope_isolation:/target> {
    define the position<dup_local>.
    it happens when {
        the position<dup_local> has a dimension point.
    } and it does {
        define the position<_noop>.
        create a dimension point in position<_noop>.
    }
}
