define the potential action<mv:define-lang.org:test_dim_five_chain:/act_a> {
    define the position<pos_b> {
        it may only contain dimension points where {
            it has the action</act_b>.
        }
    }
    it happens when {
        the position<pos_b> has a dimension point.
    } and it does {
    }
}
