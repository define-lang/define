define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</start>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</start>::position<pp>.
    }
}
