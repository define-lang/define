define the potential position<my.domain.com:my_lib:/host> {
    it also assigns the action</helper>.
    after it is assigned {
        create a dimension point in action</helper>::position<run>.
    }
}
