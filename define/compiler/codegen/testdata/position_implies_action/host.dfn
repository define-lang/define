define the potential action<my.domain.com:my_lib:/host> {
    it also assigns the action</helper>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</helper>::position<run>.
    }
}
