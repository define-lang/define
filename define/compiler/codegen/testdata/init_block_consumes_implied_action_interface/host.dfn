define the potential position<my.domain.com:my_lib:/host> {
    it also assigns the action</worker>.
    after it is assigned {
        create a dimension point in action</worker>::position<input>.
        create a dimension point in action</worker>::position<run>.
    }
}
