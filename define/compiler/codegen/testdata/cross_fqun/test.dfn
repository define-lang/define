define the potential action<my.domain.com:parent_lib:/test> {
    it also assigns the action</do_thing>.
    it also assigns the action<my.domain.com:child_lib:/do_thing>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</do_thing>::position<trigger_pos>.
        create a particle in action<my.domain.com:child_lib:/do_thing>::position<trigger_pos>.
    }
}
