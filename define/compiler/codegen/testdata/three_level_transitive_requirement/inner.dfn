define the potential action<my.domain.com:my_lib:/inner> {
    define the position<run>.
    define the position<data>.
    it happens when {
        the position<run> has a dimension point.
    } and it does {
        destroy the dimension point in position<data>.
    }
}
