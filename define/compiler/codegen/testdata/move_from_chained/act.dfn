define the potential action<my.domain.com:my_lib:/act> {
    define the position<trigger>.
    define the position<chain_src_a> {
        it may only contain particles where {
            it has the position</mid_src_a>.
        }
    }
    define the position<chain_src_b> {
        it may only contain particles where {
            it has the position</mid_src_b>.
        }
    }
    define the position<chain_src_c> {
        it may only contain particles where {
            it has the position</mid_src_c>.
        }
    }
    define the position<iface_dest>.
    define the position<chain_dest> {
        it may only contain particles where {
            it has the position</mid_dest>.
        }
    }
    it happens when {
        the position<trigger> has a particle.
    } and it does {
        define the position<local_dest>.
        move the particle in position<chain_src_a>::position</mid_src_a>::position</end_src_a> to position<local_dest>.
        move the particle in position<chain_src_b>::position</mid_src_b>::position</end_src_b> to position<iface_dest>.
        move the particle in position<chain_src_c>::position</mid_src_c>::position</end_src_c> to position<chain_dest>::position</mid_dest>::position</end_dest>.
    }
}
