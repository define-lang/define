define the potential action<my.domain.com:my_lib:/test> {
    it also assigns the action</act>.
    it happens when {
        this particle is created.
    } and it does {
        create a particle in action</act>::position<chain_dest>.
        create a particle in action</act>::position<chain_dest>::position</mid_dest>.
        create a particle in action</act>::position<trigger>.
    }
}
