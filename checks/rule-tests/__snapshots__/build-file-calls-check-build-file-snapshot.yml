id: build-file-calls-check-build-file
snapshots:
  ? |
    def _macro():
        check_build_file()
  : labels:
    - source: |
        def _macro():
            check_build_file()
      style: primary
      start: 0
      end: 37
  ? |
    filegroup(name = "testdata", srcs = glob(["**"]))
  : labels:
    - source: |
        filegroup(name = "testdata", srcs = glob(["**"]))
      style: primary
      start: 0
      end: 50
  ? |
    filegroup(name = "testdata", srcs = glob(["**"]))

    # check_build_file()
  : labels:
    - source: |
        filegroup(name = "testdata", srcs = glob(["**"]))

        # check_build_file()
      style: primary
      start: 0
      end: 72
